import React, { useEffect } from 'react';

// import { useGetMessagesQuery } from '../../services/chat';

export const SocketIoConnection: React.FC = (props) => {
  // const [message, setMessage] = useGetMessagesQuery("");
  return <div>{props.children}</div>
}